import{_ as e,p as d,q as a,a1 as i}from"./framework-5866ffd3.js";const n={},s=i(`<h2 id="linux关机方法" tabindex="-1"><a class="header-anchor" href="#linux关机方法" aria-hidden="true">#</a> Linux关机方法</h2><h3 id="_1-shutdown命令" tabindex="-1"><a class="header-anchor" href="#_1-shutdown命令" aria-hidden="true">#</a> 1.shutdown命令：</h3><p>这是一个安全的关机命令，它会以安全的方式关闭系统，并通知所有登录的用户系统即将关闭。例如，shutdown -h now 会立即关闭系统，而 shutdown -h +5 则会在5分钟后关闭系统。</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>shutdown -h now：立即关机。
shutdown -h +分钟：在指定分钟后关机。
shutdown -h 时间：在指定时间点关机。
shutdown -r now：立即重启。
shutdown -c：取消上一个关机命令。
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="_2-halt命令" tabindex="-1"><a class="header-anchor" href="#_2-halt命令" aria-hidden="true">#</a> 2.halt命令：</h3><p>这个命令会立刻关机，相当于执行 shutdown -h。它会 杀死应用进程，执行sync系统调用，然后在文件系统写操作完成后停止内核。</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>halt：停止所有进程并关闭系统，现在通常是指向reboot命令的符号链接，其效果等同于shutdown -h。
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="_3-poweroff命令" tabindex="-1"><a class="header-anchor" href="#_3-poweroff命令" aria-hidden="true">#</a> 3.poweroff命令：</h3><p>这个命令也会立刻关机，它相当于halt的软链接，会切断系统电源。</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>poweroff：立即关闭电源，等同于shutdown -h now。
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="_4-init命令" tabindex="-1"><a class="header-anchor" href="#_4-init命令" aria-hidden="true">#</a> 4.init命令：</h3><p>init是所有进程的祖先，可以通过改变运行级别来关机或重启。例如，init 0 会关机，而 init 6 会重启系统。</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>init 0：切换到运行级别0，系统将关机。
init 6：切换到运行级别6，系统将重启。
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="_5-systemctl命令" tabindex="-1"><a class="header-anchor" href="#_5-systemctl命令" aria-hidden="true">#</a> 5.systemctl命令：</h3><p>在现代的Linux发行版中，如CentOS 7及更高版本，也可以使用systemctl命令来关机，例如 systemctl poweroff 会关闭系统。</p><p>立即关机：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>systemctl poweroff
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>在指定时间后关机：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>systemctl poweroff --time=HH:MM
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>立即重启</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>systemctl reboot
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>在指定时间后重启</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>systemctl reboot --time=HH:MM
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>挂起系统（挂起模式类似于Windows的休眠，但不是完全关闭电源）</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>systemctl suspend
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>使系统休眠（保存当前状态到硬盘并关闭电源）</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>systemctl hibernate
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>注意事项</p><p>使用<code>systemctl</code>关机之前，建议执行<code>sync</code>命令，以确保所有磁盘缓存都被写入，避免数据丢失。</p><p>如果需要取消关机或重启操作，可以使用以下命令</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>systemctl stop reboot.target
systemctl stop poweroff.target
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="_6-reboot命令" tabindex="-1"><a class="header-anchor" href="#_6-reboot命令" aria-hidden="true">#</a> 6.reboot命令</h3><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>reboot：重新启动系统。
reboot -p：关机，等同于poweroff。
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="_7-sync命令" tabindex="-1"><a class="header-anchor" href="#_7-sync命令" aria-hidden="true">#</a> 7.sync命令</h3><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>sync：在执行关机命令之前使用，确保文件系统的缓冲区被写入硬盘，避免数据丢失。
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>总的来说，Linux提供了多种关机方法，可以根据具体需求和场景选择合适的方法。需要注意的是，强制关机（如直接断掉电源）可能会导致进程的数据丢失，使系统处于不稳定状态，甚至损坏硬件设备，因此不推荐使用。</p><h2 id="linux用户管理" tabindex="-1"><a class="header-anchor" href="#linux用户管理" aria-hidden="true">#</a> Linux用户管理</h2><h3 id="_1-增加用户" tabindex="-1"><a class="header-anchor" href="#_1-增加用户" aria-hidden="true">#</a> 1. 增加用户</h3><p>增加用户可以使用 useradd 命令。</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>useradd 新用户名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>​ 如果你想指定用户的家目录，可以使用 -d 选项：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>useradd -d /home/新用户目录 新用户名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>​ 如果你想同时创建用户的家目录，可以使用 -m 选项：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>useradd -m 新用户名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>此外，还可以指定用户ID（UID）：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>useradd -u 指定UID 新用户名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="_2-删除用户" tabindex="-1"><a class="header-anchor" href="#_2-删除用户" aria-hidden="true">#</a> 2. 删除用户</h3><p>删除用户使用 userdel 命令。</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>userdel 要删除的用户名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>​ 如果你同时想删除用户的主目录及邮箱等配置文件，可以使用 -r 选项：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>userdel -r 要删除的用户名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="_3-修改用户" tabindex="-1"><a class="header-anchor" href="#_3-修改用户" aria-hidden="true">#</a> 3. 修改用户</h3><p>​ 修改用户信息使用 usermod 命令。</p><p>​ 修改用户的家目录：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>usermod -d 新家目录 用户名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>修改用户所属的用户组：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>usermod -g 新用户组 用户名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>将用户添加到附加组：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>usermod -a -G 附加用户组 用户名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="_4-查询用户" tabindex="-1"><a class="header-anchor" href="#_4-查询用户" aria-hidden="true">#</a> 4. 查询用户</h3><p>查看所有用户信息：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>cat /etc/passwd
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>查看特定用户信息：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>id 用户名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>或者你可以使用 getent 命令查看用户信息：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>getent passwd 用户名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="_5-设置用户密码" tabindex="-1"><a class="header-anchor" href="#_5-设置用户密码" aria-hidden="true">#</a> 5. 设置用户密码</h3><p>新创建的用户需要设置密码后才能登录。设置密码使用 passwd 命令。</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>passwd 用户名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>系统会提示你输入并确认密码。</p><h3 id="_6-查看当前用户" tabindex="-1"><a class="header-anchor" href="#_6-查看当前用户" aria-hidden="true">#</a> 6. 查看当前用户</h3><p>查看当前登录的用户可以使用以下命令：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>whoami
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>或者：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>echo \\$USER
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="_7-切换用户" tabindex="-1"><a class="header-anchor" href="#_7-切换用户" aria-hidden="true">#</a> 7. 切换用户</h3><p>切换用户可以使用 su 命令。</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>su - 用户名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="linux用户组管理" tabindex="-1"><a class="header-anchor" href="#linux用户组管理" aria-hidden="true">#</a> Linux用户组管理</h2><p>Linux 系统中，用户组管理同样非常重要，它有助于系统管理员对用户进行分类管理，控制不同用户对系统资源的访问。以下是一些基本的用户组管理命令：</p><h3 id="_1-创建用户组" tabindex="-1"><a class="header-anchor" href="#_1-创建用户组" aria-hidden="true">#</a> 1. 创建用户组</h3><p>使用 <code>groupadd</code> 命令创建一个新的用户组。</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>groupadd 新用户组名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果你想指定用户组的 GID（Group ID），可以使用 <code>-g</code> 选项：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>groupadd -g 指定GID 新用户组名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="_2-删除用户组" tabindex="-1"><a class="header-anchor" href="#_2-删除用户组" aria-hidden="true">#</a> 2. 删除用户组</h3><p>删除用户组使用 <code>groupdel</code> 命令。</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>groupdel 要删除的用户组名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>注意：如果用户组中有用户，你需要先删除或移动这些用户，否则无法删除用户组。</p><h3 id="_3-修改用户组" tabindex="-1"><a class="header-anchor" href="#_3-修改用户组" aria-hidden="true">#</a> 3. 修改用户组</h3><p>修改用户组信息使用 <code>groupmod</code> 命令。</p><ul><li><p>修改用户组的名称：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>groupmod -n 新用户组名 旧用户组名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li><li><p>修改用户组的 GID：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>groupmod -g 新GID 用户组名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li></ul><h3 id="_4-查看用户组" tabindex="-1"><a class="header-anchor" href="#_4-查看用户组" aria-hidden="true">#</a> 4. 查看用户组</h3><p>查看所有用户组信息：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>cat /etc/group
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>查看特定用户组信息：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>getent group 用户组名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="_5-添加用户到用户组" tabindex="-1"><a class="header-anchor" href="#_5-添加用户到用户组" aria-hidden="true">#</a> 5. 添加用户到用户组</h3><p>你可以使用 <code>usermod</code> 命令将用户添加到指定的用户组。</p><ul><li><p>将用户添加到附加组（不离开当前主组）：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>usermod -a -G 用户组名 用户名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li><li><p>更改用户的主组：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>usermod -g 用户组名 用户名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li></ul><h3 id="_6-查看用户所属的用户组" tabindex="-1"><a class="header-anchor" href="#_6-查看用户所属的用户组" aria-hidden="true">#</a> 6. 查看用户所属的用户组</h3><p>查看用户所属的用户组：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>groups 用户名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>这将列出用户所属的所有用户组。</p><h3 id="_7-切换用户组" tabindex="-1"><a class="header-anchor" href="#_7-切换用户组" aria-hidden="true">#</a> 7. 切换用户组</h3><p>在命令行中，可以使用 <code>newgrp</code> 命令来切换到另一个组。</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>newgrp 用户组名
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>这将在当前会话中切换到新的用户组。</p><p>用户组管理对于文件权限和系统资源的控制至关重要。在管理用户组时，请确保理解每个命令的影响，并根据组织的安全策略和资源管理需求进行操作。此外，还需要定期审计用户和用户组设置，以确保系统安全性和合规性。</p><h2 id="linux-文件与目录操作" tabindex="-1"><a class="header-anchor" href="#linux-文件与目录操作" aria-hidden="true">#</a> Linux-文件与目录操作</h2><h3 id="_1-ls-列出目录内容" tabindex="-1"><a class="header-anchor" href="#_1-ls-列出目录内容" aria-hidden="true">#</a> 1. <code>ls</code> - 列出目录内容</h3><ul><li><code>ls</code>：列出当前目录下的文件和子目录。</li><li><code>ls -a</code>：显示所有文件，包括隐藏文件（以<code>.</code>开头的文件）。</li><li><code>ls -l</code>：以详细列表形式显示文件权限、拥有者、大小等信息。</li><li><code>ls -h</code>：与 <code>-l</code> 一起使用时，以易读的格式（如KB、MB）显示文件大小。</li><li><code>ls -R</code>：递归列出所有子目录。</li></ul><h3 id="_2-cd-切换目录" tabindex="-1"><a class="header-anchor" href="#_2-cd-切换目录" aria-hidden="true">#</a> 2. <code>cd</code> - 切换目录</h3><ul><li><code>cd /path/to/directory</code>：切换到指定路径的目录。</li><li><code>cd ..</code>：切换到当前目录的父目录。</li><li><code>cd ~</code>：切换到当前用户的主目录。</li><li><code>cd -</code>：切换到上一次的工作目录。</li></ul><h3 id="_3-pwd-显示当前目录路径" tabindex="-1"><a class="header-anchor" href="#_3-pwd-显示当前目录路径" aria-hidden="true">#</a> 3. <code>pwd</code> - 显示当前目录路径</h3><ul><li><code>pwd</code>：显示当前工作目录的绝对路径。</li></ul><h3 id="_4-mkdir-创建目录" tabindex="-1"><a class="header-anchor" href="#_4-mkdir-创建目录" aria-hidden="true">#</a> 4. <code>mkdir</code> - 创建目录</h3><ul><li><code>mkdir directory</code>：创建一个名为“directory”的新目录。</li><li><code>mkdir -p directory/child</code>：递归创建多级目录。</li></ul><h3 id="_5-rmdir-删除目录" tabindex="-1"><a class="header-anchor" href="#_5-rmdir-删除目录" aria-hidden="true">#</a> 5. <code>rmdir</code> - 删除目录</h3><ul><li><code>rmdir directory</code>：删除名为“directory”的空目录。</li></ul><h3 id="_6-cp-复制文件或目录" tabindex="-1"><a class="header-anchor" href="#_6-cp-复制文件或目录" aria-hidden="true">#</a> 6. <code>cp</code> - 复制文件或目录</h3><ul><li><code>cp source destination</code>：复制文件或目录。</li><li><code>cp -r source_directory/ destination</code>：递归复制目录及内容。</li></ul><h3 id="_7-mv-移动或重命名文件或目录" tabindex="-1"><a class="header-anchor" href="#_7-mv-移动或重命名文件或目录" aria-hidden="true">#</a> 7. <code>mv</code> - 移动或重命名文件或目录</h3><ul><li><code>mv source destination</code>：移动文件或目录，或重命名。</li><li>如果源和目标在同一目录下，则是重命名。</li></ul><h3 id="_8-rm-删除文件或目录" tabindex="-1"><a class="header-anchor" href="#_8-rm-删除文件或目录" aria-hidden="true">#</a> 8. <code>rm</code> - 删除文件或目录</h3><ul><li><code>rm file</code>：删除文件。</li><li><code>rm -r directory</code>：递归删除目录及其内容。</li><li><code>rm -f file</code>：强制删除文件，不会有任何提示。</li></ul><h3 id="_9-touch-创建空文件或修改文件时间戳" tabindex="-1"><a class="header-anchor" href="#_9-touch-创建空文件或修改文件时间戳" aria-hidden="true">#</a> 9. <code>touch</code> - 创建空文件或修改文件时间戳</h3><ul><li><code>touch file</code>：如果文件不存在，则创建一个空文件；如果存在，则更新其时间戳。</li></ul><h3 id="_10-chmod-更改文件权限" tabindex="-1"><a class="header-anchor" href="#_10-chmod-更改文件权限" aria-hidden="true">#</a> 10. <code>chmod</code> - 更改文件权限</h3><ul><li><code>chmod u+x file</code>：给文件拥有者增加执行权限。</li><li><code>chmod g-w file</code>：去除文件所在组的写权限。</li><li><code>chmod o-r file</code>：去除其他用户的读权限。</li></ul><h3 id="_11-chown-更改文件拥有者或组" tabindex="-1"><a class="header-anchor" href="#_11-chown-更改文件拥有者或组" aria-hidden="true">#</a> 11. <code>chown</code> - 更改文件拥有者或组</h3><ul><li><code>chown user:group file</code>：更改文件的拥有者和组。</li><li><code>chown user file</code>：只更改文件的拥有者。</li></ul><h3 id="_12-chgrp-更改文件所属组" tabindex="-1"><a class="header-anchor" href="#_12-chgrp-更改文件所属组" aria-hidden="true">#</a> 12. <code>chgrp</code> - 更改文件所属组</h3><ul><li><code>chgrp group file</code>：更改文件所属的组。</li></ul>`,134),r=[s];function l(t,c){return d(),a("div",null,r)}const o=e(n,[["render",l],["__file","index.html.vue"]]);export{o as default};
