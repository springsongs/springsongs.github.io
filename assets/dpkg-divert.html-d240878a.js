import{_ as e,p as a,q as s,a1 as n}from"./framework-5866ffd3.js";const i={},d=n(`<h1 id="dpkg-divert" tabindex="-1"><a class="header-anchor" href="#dpkg-divert" aria-hidden="true">#</a> dpkg-divert</h1><p>Debian Linux中创建并管理一个转向列表</p><h2 id="补充说明" tabindex="-1"><a class="header-anchor" href="#补充说明" aria-hidden="true">#</a> 补充说明</h2><p><strong>dpkg-divert命令</strong> 是Debian Linux中创建并管理一个转向（diversion）列表，其使得安装文件的默认位置失效的工具。</p><h3 id="语法" tabindex="-1"><a class="header-anchor" href="#语法" aria-hidden="true">#</a> 语法</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>dpkg-divert<span class="token punctuation">(</span>选项<span class="token punctuation">)</span><span class="token punctuation">(</span>参数<span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="选项" tabindex="-1"><a class="header-anchor" href="#选项" aria-hidden="true">#</a> 选项</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>--add：添加一个转移文件；
--remove：删除一个转移文件；
--list：列出匹配的转移；
--truename：对应转移文件真实文件名；
--quidet：安静模式。
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="参数" tabindex="-1"><a class="header-anchor" href="#参数" aria-hidden="true">#</a> 参数</h3><p>文件：指定转移文件名。</p><h3 id="实例" tabindex="-1"><a class="header-anchor" href="#实例" aria-hidden="true">#</a> 实例</h3><p>指定软件包wibble安装时，写入<code>/usr/bin/example.foo</code>，而不是<code>/usr/bin/example</code>：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>dpkg-divert <span class="token parameter variable">--package</span> wibble <span class="token parameter variable">--divert</span> /usr/bin/example.foo <span class="token parameter variable">--rename</span> /usr/bin/example
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>指定软件包wibble安装时，删除对<code>/usr/bin/example</code>的转移修改：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>dpkg-divert <span class="token parameter variable">--package</span> wibble <span class="token parameter variable">--rename</span> <span class="token parameter variable">--remove</span> /usr/bin/example
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>删除对<code>/usr/bin/example</code>的转移修改：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>dpkg-divert <span class="token parameter variable">--rename</span> <span class="token parameter variable">--remove</span> /usr/bin/example
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>添加一个软件包安装时，写入<code>/usr/bin/example.foo</code>，而不是<code>/usr/bin/example</code>的修改：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>dpkg-divert <span class="token parameter variable">--divert</span> /usr/bin/example.foo <span class="token parameter variable">--rename</span> /usr/bin/example
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,19),r=[d];function l(c,t){return a(),s("div",null,r)}const o=e(i,[["render",l],["__file","dpkg-divert.html.vue"]]);export{o as default};
