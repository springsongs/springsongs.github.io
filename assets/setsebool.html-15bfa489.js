import{_ as a,p as e,q as s,a1 as n}from"./framework-5866ffd3.js";const l={},i=n(`<h1 id="setsebool" tabindex="-1"><a class="header-anchor" href="#setsebool" aria-hidden="true">#</a> setsebool</h1><p>修改SElinux策略内各项规则的布尔值</p><h2 id="补充说明" tabindex="-1"><a class="header-anchor" href="#补充说明" aria-hidden="true">#</a> 补充说明</h2><p><strong>setsebool命令</strong> 是用来修改SElinux策略内各项规则的布尔值。setsebool命令和getsebool命令是SELinux修改和查询布尔值的一套工具组。SELinux的策略与规则管理相关命令：seinfo命令、sesearch命令、getsebool命令、setsebool命令、semanage命令。</p><h3 id="语法" tabindex="-1"><a class="header-anchor" href="#语法" aria-hidden="true">#</a> 语法</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token punctuation">[</span>-P<span class="token punctuation">]</span> 布尔值<span class="token operator">=</span><span class="token punctuation">[</span><span class="token number">0</span><span class="token operator">|</span><span class="token number">1</span><span class="token punctuation">]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="选项" tabindex="-1"><a class="header-anchor" href="#选项" aria-hidden="true">#</a> 选项</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>-P:直接将设置值写入配置文件，该设置数据将来会生效的。
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h3 id="实例" tabindex="-1"><a class="header-anchor" href="#实例" aria-hidden="true">#</a> 实例</h3><p>允许vsvtp匿名用户写入权限：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> <span class="token assign-left variable">allow_ftpd_anon_write</span><span class="token operator">=</span><span class="token number">1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果你希望你的ftp用户可以访问自己的家目录的话，需要开启：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> ftp_home_dir <span class="token number">1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果你希望将vsftpd以daemon的方式运行的话，需要开启：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> ftpd_is_daemon <span class="token number">1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>你可以让SElinux停止保护vsftpd的daemon方式动行：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> ftpd_disable_trans <span class="token number">1</span> 
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>HTTP被设置允许cgi的设置：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> httpd_enable_cgi <span class="token number">1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>允许用户HHTP访问其家目录，该设定限仅于用户的家目录主页：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> httpd_enable_homedirs <span class="token number">1</span>
chcon <span class="token parameter variable">-R</span> <span class="token parameter variable">-t</span> httpd_sys_content_t ~user/public_html
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>允许httpd访问终端：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> httpd_tty_comm <span class="token number">1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>关闭Selinux的关于httpd进程守护的保护：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> httpd_disable_trans <span class="token number">1</span>
<span class="token function">service</span> httpd restart
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>关于named、master更新selinux设定：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> named_write_master_zones <span class="token number">1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>关闭named的进程守护保护：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> named_disable_trans <span class="token number">1</span>
<span class="token function">service</span> named restart
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>Selinux将本机的NFS共享设置成只读：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> nfs_export_all_ro <span class="token number">1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>SElinux将本机的NFS共享设置成可读可写：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> nfs_export_all_rw <span class="token number">1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果你想要将远程NFS的家目录共享到本机，需要开启：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> use_nfs_home_dirs <span class="token number">1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果samba服务器共享目录给多个域，则需要：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> <span class="token assign-left variable">allow_smbd_anon_write</span><span class="token operator">=</span><span class="token number">1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>samba服务器要共享家目录时：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> samba_enable_home_dirs <span class="token number">1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果你需在本机上使用远程samba服务器的家目录：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> use_samba_home_dirs <span class="token number">1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>关闭selinux关于samba的进程守护的保护：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> smbd_disable_trans <span class="token number">1</span>
<span class="token function">service</span> smb restart
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>允许rsync其他用户写入时：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> <span class="token assign-left variable">allow_rsync_anon_write</span><span class="token operator">=</span><span class="token number">1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>停止rsync的进程保护</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> rsync_disable_trans <span class="token number">1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>允许系统使用kerberos：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> allow_kerberos <span class="token number">1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>系统工作在nis环境时：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>setsebool <span class="token parameter variable">-P</span> allow_ypbind <span class="token number">1</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,51),d=[i];function r(t,p){return e(),s("div",null,d)}const c=a(l,[["render",r],["__file","setsebool.html.vue"]]);export{c as default};
